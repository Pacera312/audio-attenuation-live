/**\n * Service pour récupérer les données météo via Open-Meteo API\n * Aucune clé API requise\n */\n\nexport interface WeatherData {\n  temperature: number\n  humidity: number\n  location: string\n  timestamp: Date\n}\n\nexport interface GeocodeResult {\n  name: string\n  latitude: number\n  longitude: number\n  country: string\n}\n\n/**\n * Récupère les coordonnées d'une localisation par son nom\n * @param location Nom de la ville/région\n * @returns Coordonnées et informations de localisation\n */\nexport async function geocodeLocation(location: string): Promise<GeocodeResult> {\n  const response = await fetch(\n    `https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(\n      location\n    )}&count=1&language=en&format=json`\n  )\n\n  if (!response.ok) {\n    throw new Error('Erreur lors de la géolocalisation')\n  }\n\n  const data = await response.json()\n\n  if (!data.results || data.results.length === 0) {\n    throw new Error(`Localisation \"${location}\" non trouvée`)\n  }\n\n  const result = data.results[0]\n  return {\n    name: result.name,\n    latitude: result.latitude,\n    longitude: result.longitude,\n    country: result.country || '',\n  }\n}\n\n/**\n * Récupère les données météo actuelles pour des coordonnées\n * @param latitude Latitude\n * @param longitude Longitude\n * @param location Nom de la localisation (pour affichage)\n * @returns Données météo actuelles\n */\nexport async function getCurrentWeather(\n  latitude: number,\n  longitude: number,\n  location: string\n): Promise<WeatherData> {\n  const response = await fetch(\n    `https://api.open-meteo.com/v1/forecast?latitude=${latitude}&longitude=${longitude}&current=temperature_2m,relative_humidity_2m&timezone=auto`\n  )\n\n  if (!response.ok) {\n    throw new Error('Erreur lors de la récupération des données météo')\n  }\n\n  const data = await response.json()\n  const current = data.current\n\n  return {\n    temperature: current.temperature_2m,\n    humidity: current.relative_humidity_2m,\n    location,\n    timestamp: new Date(),\n  }\n}\n\n/**\n * Récupère la météo pour une localisation par son nom\n * @param location Nom de la ville/région\n * @returns Données météo actuelles\n */\nexport async function getWeatherByLocation(location: string): Promise<WeatherData> {\n  try {\n    const coords = await geocodeLocation(location)\n    return getCurrentWeather(coords.latitude, coords.longitude, coords.name)\n  } catch (error) {\n    throw new Error(\n      `Impossible de récupérer la météo pour \"${location}\": ${error instanceof Error ? error.message : 'Erreur inconnue'}`\n    )\n  }\n}\n\n/**\n * Récupère la météo selon la géolocalisation du navigateur\n * @returns Données météo actuelles\n */\nexport async function getWeatherByGeolocation(): Promise<WeatherData> {\n  return new Promise((resolve, reject) => {\n    if (!navigator.geolocation) {\n      reject(new Error('Géolocalisation non disponible dans ce navigateur'))\n      return\n    }\n\n    navigator.geolocation.getCurrentPosition(\n      async (position) => {\n        try {\n          const { latitude, longitude } = position.coords\n          const weather = await getCurrentWeather(\n            latitude,\n            longitude,\n            'Position actuelle'\n          )\n          resolve(weather)\n        } catch (error) {\n          reject(error)\n        }\n      },\n      (error) => {\n        reject(\n          new Error(\n            `Erreur de géolocalisation: ${error.message}`\n          )\n        )\n      }\n    )\n  })\n}\n"